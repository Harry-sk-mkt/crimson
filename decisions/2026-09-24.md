## Gemini 리서치 연동 — API 키 발급으로 해결, CLI/MCP 불필요 — Claude Code

**무엇을 결정/리서치했는지**
- 2026-09-22에 막혔던 "블로그 리서치를 Gemini에 맡기기" 재시도. 그때 막힌 지점은 `gemini` CLI(`@google/gemini-cli`) 개인 무료 티어(`IneligibleTierError`)였음 — 이번엔 사용자가 Google AI Pro/Ultra 구독 중이라 CLI의 Google 계정 로그인(GCA)을 재시도했으나, `antigravity.cmd`/`agy.cmd` 바이너리를 요구하는데 실제 설치된 건 `antigravity-ide.cmd`(이름 불일치, 비공식/미문서화 연동이라 더 파고들 가치 낮다고 판단해 중단).
- 대신 **aistudio.google.com에서 Gemini API 키 직접 발급** → `curl`로 `generateContent` REST 호출 테스트 성공(모델 `gemini-3.8-flash` 응답 확인). CLI도, MCP 서버도 불필요 — REST 호출 한 번이면 충분한 것으로 결론.
- 키는 Windows 사용자 환경변수 `GEMINI_API_KEY`로 저장(`setx`, 새 터미널부터 적용). 이 파일/코드베이스 어디에도 키 값 자체는 기록하지 않음.

**왜**
- CLI의 Google 계정 로그인 경로는 Antigravity와의 비공식 내부 연동에 의존하는 것으로 보여 문서화도 안 돼 있고 재현성이 낮음 — API 키 직접 발급이 더 단순하고 확실한 경로.
- 무료 티어(API 키만, 결제 미연결) 사용 시 프롬프트가 구글 모델 개선에 쓰일 수 있다는 점을 사용자에게 안내함 — 민감한 사내 정보는 이 경로로 안 보내는 게 안전.

**다음 액션**
- 블로그 리서치에 실제로 언제/어떻게 Gemini를 호출할지(예: `pillar-planner`/`researcher` 작업 중 특정 소스 교차검증이 필요할 때 `curl`로 직접 호출) 다음 블로그 세션에서 구체화.

## 세션 규칙 정비 — Claude Code

**무엇을 결정/리서치했는지**
- crimson 루트 로컬 저장소가 origin보다 27커밋 뒤처져 있던 원인 확인: 2026-09-23 Mac에서 "세션 규칙 루트 승격"(`6fbe5f5`)까지 push했는데 Windows 로컬은 그 뒤로 pull 안 됨. `git pull`로 동기화 완료.
- `decisions/`에 이미 있던 "세션 종료 멘트가 유일한 로그 트리거" 구조의 gap을 확인: 종료 멘트 없이 세션이 끊기면 그 세션 내용이 아예 안 남음.
- 새 규칙 **Real-Time Decision/Change Log** 도입: 의미 있는 결정/변경이 생기면 세션 종료를 기다리지 않고 즉시 `decisions/YYYY-MM-DD.md`(또는 `lead-tracker/docs/Changelog.md`)에 남긴다. 커밋/푸시는 기존처럼 세션 종료 시점에 모아서 진행.
- 기록 단위는 원문 대화록이 아니라 기존 `decisions/CLAUDE.md` 형식 그대로 **요약 단위**(무엇을/왜/다음 액션)로 확정.
- `~/.claude/CLAUDE.md`(전역, 신규 생성)에 두 가지 세션 규칙 추가: (1) 세션 시작 시 또는 "뭐부터 해야하지" 질문 시 `~/.claude/TODO.md` 우선 확인, (2) "기록해줘"만 말하면 기본적으로 `~/.claude/TODO.md`에 추가.

**왜**
- 로컬 저장소가 낡은 상태로 방치되면 사용자 질문에 오래된 정보로 답하는 사고가 반복됨 (lead-tracker 2026-07-24, 2026-09-02 선례와 동일 패턴).
- 세션 종료 멘트 유무에 의존한 로깅은 비정상 종료 시 기록 유실 위험이 있음.

**다음 액션**
- ~~`crimson/CLAUDE.md`, `decisions/CLAUDE.md` 변경사항 커밋 필요~~ → 완료 (`446e5d7`, `4f782c5`).
- 이번에 만들었다 삭제한 `crimson/scripts/`, `blog/scripts/` 관련 후속 작업 없음 — 공식 규칙(수동 fetch/divergence 확인)으로 충분하다고 결론.
- 이어서 Session-End Notion Daily Update 규칙(db_Daily 오늘 페이지 없으면 생성) 추가 완료.

## 이월 항목 완료 보고

- **Antigravity dmg/Dock 정리(Mac)** 완료 — 2026-09-23 이월 항목 해소.
- **블로그 9/25~9/27(금~일) 발행분 포스팅 완료** — 9/23 세션에서 작성·style-guard 검증·Notion "Ready to Publish" 상태까지 마쳤던 세 편의 네이버 실제 발행이 끝남.

## Ponytail 미설치 확인 및 재설치

- `decisions/2026-09-23.md`에는 "Ponytail 설치 완료(user scope)"로 기록돼 있었으나, `claude plugin list`로 실제 확인한 결과 **설치돼 있지 않았음**(marketplace도 공식 것만 등록, `settings.json`에 관련 항목 전혀 없음). 09-23 로그가 실제 반영과 어긋난 사례 — 원인은 미확인(설치 명령이 그 세션에서 실패했는데 로그만 성공으로 남았을 가능성).
- `claude plugin marketplace add DietrichGebert/ponytail` → `claude plugin install ponytail@ponytail` 재실행, `claude plugin list`로 `ponytail@ponytail` v4.10.0 user scope enabled 확인 완료. `settings.json`에 `extraKnownMarketplaces`/`enabledPlugins` 반영됨.
- 적용은 이전과 동일하게 **다음 세션(재시작)부터**.

**다음 액션**
- 다음 세션 시작 시 Ponytail 훅(SessionStart 등)이 실제로 동작하는지 확인.

## lead-tracker: Ad Spend Weekly 캐시 지연 원인 분석 (수정 전)

사용자 제공 Executions 로그(09-24 05:16~05:34) 분석 결과:
- `refreshAdSpendCache_()`(월별) 안 Meta 처리 구간 추정 ~10분 21초(05:16:41 Kakao 완료 → 05:27:02 Naver 로그 시작 사이, 별도 lap 로그 없음).
- `refreshAdSpendWeeklyCache_()`(주별)의 `[AdSpendWeekly timing]` lap: `Meta_Raw 읽기+주별 분배` 356.4초가 전체 374초 중 대부분 차지, 나머지(Naver/Kakao/환율/쓰기)는 모두 20초 미만.
- 근본 원인 두 가지: (1) `readMetaRawRows_()`(외부 Meta_Raw 스프레드시트 `openById()`+전체 읽기)가 **매 실행마다 Meta_Raw 전체 히스토리(FY23 SEP~현재)를 처음부터 다시 읽고 재집계** — Target_REP/FY_REP에 이미 있는 체크포인트 기반 증분 캐싱(`_LAST_ROW`/`_CACHE`) 패턴이 Ad Spend에는 아직 없음. 사용자 확인: Meta 데이터는 3일 주기로만 append되므로 대부분의 4시간 주기 실행에서 신규 행이 0건인데도 전체 재계산이 발생하는 구조. (2) 같은 실행 안에서 월별(`computeMetaSpendSummary_`)·주별(`computeMetaSpendWeeklySummary_`)이 `readMetaRawRows_()`를 각각 한 번씩, 총 두 번 중복 호출(09-23 Changelog에 이미 지적돼 있던 부분).
- `sheetToObjects()`의 청크 읽기(`getRangeValuesChunked_`, 8000행/청크) 자체는 2026-09-03 실측(8.7만 행/103초)상 이 정도 지연의 직접 원인일 가능성은 낮음 — Meta_Raw 실제 행 수/외부 스프레드시트에 무거운 수식이 있는지는 미확인.

**다음 액션**: ~~근본 수정 착수 전 사용자 승인 대기~~ → 아래 항목으로 이어짐.

## lead-tracker: Ad Spend 캐시 근본 수정 — 설계 방향 확정, 착수 전 read/aggregate 분리 측정 — Claude Code

**무엇을 결정/리서치했는지**
- 사용자에게 수정 범위 확인 → "증분 캐싱까지(근본 수정)" 선택.
- 코드 조사 중 위험 발견: `AD_002_Meta.js`의 `mergePreciseMetaRecordsForCampaignWeek_()`가 같은 캠페인의 분할 export 배치(예: 월~수/수~토 두 번에 걸친 export)를 같은 집계 호출 안에서 병합함(2026-09-09 과다집계 버그의 회귀 테스트로 이미 보호돼 있음, `testMergePreciseMetaRecordsForCampaignWeek`). ICFunnel/UTM_Program_Dictionary와 동일한 "마지막 체크포인트 이후 신규 행만 읽어 기존 캐시에 증분" 패턴을 그대로 쓰면, 같은 캠페인의 형제 행이 체크포인트 경계 앞뒤로 갈릴 때 병합 로직이 형제 행을 못 보게 되어 그 버그가 조용히 재발할 위험. Meta 데이터가 3일 주기로 append된다는 점에서 이론적 엣지케이스가 아니라 실제로 반복될 패턴으로 판단.
- 사용자에게 재확인 → "최근 N개월만 전체 재읽기(또는 재집계), 그 이전은 고정 캐시" 방향 확정(캠페인 단위 체크포인트 인덱싱보다 단순·안전).
- 다만 이 windowed 설계가 실제로 병목을 해소하는지는 read(I/O)와 aggregate(분배 연산) 중 무엇이 356초의 실제 병목인지 분리 측정 전에는 확정할 수 없다고 판단 — 바로 구현 착수하지 않고 `AD_004_SpendCache.js`(v1.8.0)에 read/aggregate 분리 lap 로그만 먼저 추가(계산/출력 무변경, `docs/Changelog.md` 2026-09-24 항목 참고). `safe-clasp-push.sh`로 push 완료.

**왜**
- No Assumptions/TDD 원칙상 병목을 실측하지 않고 아키텍처(캠페인 단위 vs 최근 N개월 windowed)를 먼저 확정하면, read가 실제 병목일 경우 "재집계만 줄이는" 설계는 효과가 없을 수 있음.
- 분할 배치 병합 버그는 이미 한 번 실제로 터진 전례(2026-09-09)가 있어, 같은 클래스의 버그를 증분 캐싱으로 재도입하지 않는 것이 최우선.

**다음 액션**
- ~~사용자가 두 함수 실행해 lap 로그 확인~~ → 완료, 아래 항목으로 이어짐.

## lead-tracker: Ad Spend 캐시 지연 — "Meta_Raw 크기" 가설 폐기, 외부 API 간헐 지연으로 재판정 — Claude Code

**무엇을 결정/리서치했는지**
- 사용자가 `runRefreshAdSpendWeeklyCache()`/`runRefreshAdSpendCache()`/`runSyncKakaoMomentsReportToKakaoSMSRaw()` 개별 실행 → 전부 baseline 정상(Meta_Raw 읽기 3.3~5.3초, 분배 연산 0.0~6.5초, Kakao Moments sync 13초, Naver 7.3~28초). 이전 356.4초/~10분21초와 전혀 재현 안 됨.
- `periodicRefreshAdSpendCache_` Executions 로그를 09-23~09-24 구간 필터 없이 전체 대조 → 6회 중 4회 정상(70~230초), 2회만 극단(792.7초/1093.2초), 이 2회 시각에 다른 트리거 겹침 없음(사용자 확인 — 필터 없는 전체 목록).
- **결론**: Meta_Raw 전체 히스토리 재읽기가 구조적 병목이라는 애초 가설(이 문서 앞선 항목)이 틀렸음. 개별 구성요소 실측치를 합산해도 baseline과 일치 — 코드/데이터량 문제가 아니라 외부 API(Kakao Moments/Naver/Sheets `openById()`) 쪽 간헐적 지연(6회 중 2회, 재현 불가)이 유력 원인. "최근 N개월만 재집계+과거 고정 캐시" 설계 방향은 **폐기** — 원인이 다르므로 그 구조로는 스파이크를 못 고침.
- `periodicRefreshAdSpendCache_()`에 3단계(Kakao Moments/월별/주별) 구간 로그(`[PeriodicAdSpend timing]`) 추가(`AD_004_SpendCache.js` v1.9.0) — 개별 수동 실행으론 재현 안 되니 다음 자연 발생 스파이크 때 어느 외부 호출인지 잡기 위함. push 완료.

**왜**
- 병목을 실측하지 않고 아키텍처부터 확정했다면 (이전 세션에서 우려했던 대로) 엉뚱한 문제를 고칠 뻔함 — read/aggregate 분리 측정 → 전체 파이프라인 겹침 대조 → 개별 컴포넌트 재현 시도까지 3단계 진단을 거쳐서야 "코드가 원인이 아니다"를 확정.

**다음 액션**
- 다음 4시간 주기 실행 중 792~1093초급 스파이크 재발 시 `[PeriodicAdSpend timing]` 로그로 어느 외부 API 구간인지 확정 → 그 호출에 한해 타임아웃/재시도 검토(캐싱 접근 아님).
- 원인 확정되면 v1.7.0~v1.9.0에 쌓인 진단 lap 로그 전부 정리(제거).

## 제주도 캠페인 성과 — 사용자 직접 확인 (Claude Code 미관여)

**무엇을 결정/리서치했는지**
- 9/22 오후부터 멈춰있던 제주도 캠페인이 재개됨. 재개 후 2일간 P1 3건 발생(그중 New P1 1건 신규 유입) — 사용자가 별도로(Claude Code 밖에서) 확인.

**왜**
- (배경 미기재 — 사용자가 결과만 전달)

**다음 액션**
- 없음(현황 기록용).

## Session-End Notion Daily Update 규칙 보강 — 신규 페이지의 Jennie/Richard "Day off" 처리

**무엇을 결정/리서치했는지**
- `crimson/CLAUDE.md`의 Session-End Notion Daily Update 규칙 수정: 기존엔 "Jennie/Richard 섹션은 건드리지 않는다"만 있었는데, **오늘 날짜 페이지를 템플릿으로 신규 생성하는 경우에 한해** 템플릿 기본값이 남은 Jennie/Richard 섹션을 지우고 각각 "Day off"로 남기도록 변경. 기존에 이미 있던 페이지는 계속 그대로(안 건드림).

**왜**
- 사용자 요청(2026-09-24) — 신규 생성 시 템플릿 잔여값이 그대로 남는 걸 방지.

**다음 액션**
- 다음 세션 종료 시 신규 페이지 생성 케이스가 발생하면 이 규칙대로 동작하는지 확인.

## 세션 규칙 전역 포인터 추가 — cwd가 crimson 밖이어도 규칙 적용 — Claude Code

**무엇을 결정/리서치했는지**
- 오늘 오전 세션이 `projects/` 루트(상위 폴더)에서 시작돼 `crimson/CLAUDE.md`가 자동 로드되지 않았고, "오늘은 여기까지하자"에 아무 세션 종료 규칙도 안 걸린 사고 발생. 원인 진단 후 시작/진행중/종료 3단계 규칙 자체(Git Sync Check, Real-Time Decision/Change Log, Session-End Commit & Push, Summary Format, Notion Daily Update)는 이미 빠짐없이 커버돼 있음을 확인 — 문제는 규칙 내용이 아니라 파일이 로드되는 시점(cwd 의존)이었음.
- 해결 방식으로 두 가지(전역에 규칙 전체 복사 vs 전역엔 포인터 한 줄만 추가) 중 사용자가 **포인터만 추가**를 선택. `~/.claude/CLAUDE.md`에 "세션 시작 시 cwd와 무관하게 항상 `projects/crimson/CLAUDE.md`를 먼저 읽고 그 안의 세션 규칙을 따른다"는 한 줄 추가 완료. 원본 규칙은 `crimson/CLAUDE.md`/`decisions/CLAUDE.md`에 그대로 두고 단일 소스 유지(중복 시 drift 위험 회피).

**왜**
- 사용자가 앞으로 세션을 항상 crimson 폴더에서 시작할 계획이지만, 오늘처럼 다른 진입 경로(단축 아이콘 등)로 상위 폴더에서 시작될 가능성에 대한 안전망 필요.
- 전체 복사 방식은 crimson 쪽 규칙이 바뀔 때마다 전역 파일도 따로 고쳐야 해서 두 파일이 어긋날 위험이 있어 기각.

**다음 액션**
- 없음(적용 완료). 다음에 crimson 폴더 밖에서 세션이 시작되는 경우 실제로 `crimson/CLAUDE.md`가 잘 로드되는지 확인.
