## 새벽 — Claude Code (Windows)

**세션 복구**: 직전 세션(`31b39a88`, 09-23 06:11 종료)이 IDE 종료로 끊김. transcript 확인 결과 파일 변경/유실 없음. 끊기기 전 진행 상황:
- Jev AI 검토 내역: claude.ai "IDE Migration" 프로젝트의 "Jev AI 프로젝트 분석 및 활용" 채팅에서 찾아 정리 완료 (활용 후보는 제안 단계, 확정 아님).
- Slack FU 리마인드: 현재 연결된 도구로는 Slack 자동 조회 불가 결론. Claude Tag(Slack 앱)는 방향이 반대라 해당 없음. FU 디렉토리 신설은 사용자가 **보류**.
- `/auto-mode-setup`: `~/.claude/settings.json`에 저장 완료 (06:32). 직전 세션이 "아직 진행 중"이라 안내한 건 오류였음.
- 마지막 요청(아래 8개 도구 분석)이 Grep 직후 끊겨 이번 세션에서 이어서 처리.

**리서치: GitHub 도구 8종 분석** (스타 수 등은 제3자 사이트 기준, 미검증)

| 이름 | 무엇인가 | 대표 저장소 | crimson 적합도 |
|---|---|---|---|
| HyperFrames | HTML/CSS 애니메이션 → 결정적 MP4 렌더링 프레임워크 (HeyGen, Apache 2.0) | heygen-com/hyperframes | 중간 — 마케팅 숏폼/런칭 영상 제작 시 |
| Taste | AI 생성 UI의 generic "slop" 방지 디자인 스킬 | Leonxlnx/taste-skill | 낮음 — 프론트엔드 작업 거의 없음 |
| Graphify | 코드베이스+문서 → 지식 그래프. Claude Code에 PreToolUse 훅 설치 | Graphify-Labs/graphify | 낮음~중간 — 저장소 규모가 작음 |
| OmniRoute | 멀티 LLM 제공자 단일 엔드포인트 로컬 게이트웨이, 무료 모델 자동 fallback (MIT) | diegosouzapw/OmniRoute | 주의 |
| ECC | Everything Claude Code — 에이전트 68/스킬 292/훅·메모리·보안 스캔 대형 프레임워크 | affaan-m/ECC | 주의 |
| claudevideo | 단일 공식 저장소 없음. 가장 근접: `/watch`로 영상 다운로드·프레임 추출·전사 | bradautomates/claude-video | 중간 — 경쟁사 광고/웨비나 영상 분석 |
| Headroom | 도구 출력·로그·JSON을 LLM 전달 전 압축, 원문 복원 가능 (라이브러리/프록시/MCP) | headroomlabs-ai/headroom | 낮음~중간 |
| Ponytail | 새 코드 대신 기존 코드·stdlib 재사용 강제 (YAGNI) 스킬 | DietrichGebert/ponytail | 중간 — 설치 간단, 위험 낮음 |

**주의 사항 (보류 근거)**:
- OmniRoute/Headroom은 모든 LLM 요청이 통과하는 프록시. 특히 OmniRoute는 무료 외부 모델로 자동 전환되므로 `analytics/data/` 리드 개인정보가 제3자 모델로 나갈 위험.
- ECC는 규칙·훅·에이전트를 대량으로 얹어 crimson `CLAUDE.md` 원칙(No Assumptions, 도메인 라우팅, 자동 push 등)과 충돌 가능. 도입한다면 필요한 스킬만 선별.
- Graphify는 PreToolUse 훅으로 파일 검색 동작을 바꾸므로 설치 전 settings 변경 내용 확인 필요.

**결정**: Ponytail 즉시 설치. HyperFrames/claude-video는 필요 시 검토. OmniRoute·ECC·Taste는 보류.

**Ponytail 설치 완료**:
- `claude plugin marketplace add DietrichGebert/ponytail` → `claude plugin install ponytail@ponytail` (v4.10.0, **user scope** — 이 PC의 모든 프로젝트에 적용).
- 구성: 스킬 6개(ponytail, -audit, -debt, -gain, -help, -review), 훅 3개(SessionStart, SubagentStart, UserPromptSubmit). 상시 토큰 비용 약 985 tok/세션.
- 훅 스크립트 검토: 외부 네트워크 호출 없음, 로컬 상태/설정 파일 쓰기만 함.
- 적용은 **다음 세션(재시작)부터**.

**다음 액션**:
- Ponytail의 "최소 코드" 성향이 crimson의 TDD 원칙(함수 작성 시 WHY 주석 + 기대값 테스트 필수)과 충돌하는지 실사용에서 관찰. 테스트를 생략하는 방향으로 가면 `CLAUDE.md` 원칙이 우선.
- 맥 환경에서도 쓰려면 맥에서 같은 두 명령으로 별도 설치 필요 (user scope는 PC별).

**미해결 (이월)**: 온보딩 페이지 분석 방식 결정, 증명사진 재시도, Vexa 로컬 전사, git commit author 불일치, Antigravity dmg/Dock 정리, FU 디렉토리(보류).

## 새벽 (이어서) — Claude Code (Windows): Jev 도입 가능성

**리서치**: TypeSafe AI의 Jev(확률/신뢰도가 붙은 타입 값을 반환하는 결정 전용 모델) 도입 가능 여부 확인.
- TypeSafe **직접 가입은 일시 중단** (수요 급증, 공식 X 공지 — 기존 가입자는 계속 사용 가능). 대기자 명단 경로는 당분간 막힘.
- **Vercel AI Gateway 경로는 가능**: `typesafe-ai/jev`, Vercel 계정의 `vck_...` 키로 대기 없이 호출. Vercel이 **2026-09-25까지 무료** 제공 (웹 검색 기준, 미검증).
- 가격(TypeSafe 직접): 입력 $0.042/100만 토큰, 출력 무과금. 요청당 최대 64k 토큰, 텍스트 전용, **영어 권장** — 한국어 정확도가 가장 큰 불확실성.
- 개인정보: TypeSafe는 고객 데이터로 학습하지 않는다고 명시하지만 외부 전송이므로 리드 개인정보(`analytics/`, lead-tracker)는 시범 단계에서 제외.

**권장 시범**: `meetings/` 회의 제목 분류(개인정보 없음)로 한국어 정확도 측정 → 쓸 만하면 학교명 별칭 매칭 등으로 확대.

**다음 액션**: 사용자 결정 대기 — Vercel 키 발급 후 무료 기간(~09-25) 안에 정확도 테스트할지.

**결과 (업데이트)**: Vercel 가입 → AI Gateway API 키 발급 → Windows 사용자 환경변수 `AI_GATEWAY_API_KEY`에 등록(`setx`) → `POST https://ai-gateway.vercel.sh/v1/evaluate` 호출 시 **403 `customer_verification_required`** — Vercel AI Gateway는 무료 크레딧/무료 모델이라도 **신용카드 등록이 필수**.

**결정**: 결제(카드 등록)가 필요하므로 Jev 도입 **보류**. 한국어 정확도 테스트도 미진행.

**남은 정리 사항**:
- 발급한 키가 세션 transcript에 평문으로 노출됨 → 사용자가 Vercel에서 **폐기 완료**.
- 환경변수 `AI_GATEWAY_API_KEY`도 **삭제 완료** (재개 시 새 키 발급 후 `! setx`로 직접 등록).
- 재개 조건: 결제 승인 시(카드 등록 + 예산 상한 $1~5 설정) 또는 TypeSafe 직접 가입 재개 시.

## 오전 — Claude Code (Windows): 온보딩 분석, git 작성자 통일, Ponytail 상태줄

- **온보딩 분석**: Gemini 대신 Claude가 직접 분석하기로. 어제 수집한 번들을 사용 (Notion 루트 `last_edited` 09-19라서 최신 확인). 결과는 `onboarding/2026-09-23-analysis.md`, 새 항목은 `TODO.md` 하단에 추가. 핵심: 리소스 허브는 좋지만 실행 레이어는 비어 있음. 완료 목표 9개 중 충분 3 / 부분 2 / 공백 4. Notion 반영(P0)은 사용자가 **나중에** 하기로 함.
- **git 작성자**: `Harry Yun <h.yun@crimsoneducation.org>`로 통일 확정. Windows global config 변경 완료. 과거 커밋은 히스토리를 다시 쓰지 않고 루트 `.mailmap`으로 합쳐서 표시. Mac은 최근 커밋이 이미 h.yun이라 조치가 필요 없어 보이지만, 다음 Mac 세션에서 `git config --global user.email` 확인 필요.
- **Ponytail 상태줄**: `~/.claude/settings.json`에 `statusLine` 추가 (재시작 후 표시).

**미해결 (이월)**: 온보딩 P0 Notion 반영, 증명사진 재시도, Vexa 로컬 전사, Antigravity dmg/Dock 정리, Mac Ponytail 설치.
- **온보딩 후속 (사용자 확인)**: 온보딩은 **2주 프로그램으로 교체** (Notion 아직 미반영, Weekly DB는 W1~W4 그대로). D1~D4는 실제로 첫째~넷째 날이라 분석의 "D=주차" 추정은 철회. `Field` 속성은 용도를 아는 사람이 없음 → 유지/삭제 결정을 TODO로. 비어 있는 콘텐츠는 전부 채울 대상이라 `onboarding/TODO.md`에 기록 (2주 구조 개편 4개, 누락 콘텐츠 3개 추가).
- **git 작성자**: 사용자가 확인함.

## 오전 (이어서) — Claude Code (Windows): lead-tracker 트리거 버그, 광고비 캐시 지연 진단

상세는 `lead-tracker/docs/Changelog.md` 2026-09-23 참고. 요약만:
- README의 09-21 FAILED 조사 → Revenue 2시간 주기, 리포트 정기 갱신 트리거가 강제종료 후 이틀간 멈춰 있던 버그 수정·복구 완료.
- 광고비 캐시 갱신이 최대 26분(30분 상한 근접) 걸림. 로그로 Naver/Kakao API는 원인이 아닌 것으로 확인. 주별 캐시에 구간별 시간 로그 추가.
- **다음 액션**: 다음 `periodicRefreshAdSpendCache_` 실행 로그(`[AdSpendWeekly timing]`)를 받아 원인 구간 확정 → 수정 → 진단 로그 제거.
- 환경 메모: 이 Windows PC의 `python`은 Store 바로가기(동작 안 함). 실제 Python 3.9는 `py`로 실행. 사용자는 그대로 두기로 함.

## 오후 — Claude Code (Windows): 세션 규칙 루트 승격, 온보딩 퀘스트 체크리스트, writing 도메인

- **규칙 정리**: lead-tracker 세션 규칙 중 공통 규칙(세션 시작 Git 동기화, 종료 시 기록·커밋·push)을 루트 `CLAUDE.md` 공통 원칙으로 승격. Apps Script 공통(파일명+함수명 명시, `_` 접미사 금지, `safe-clasp-push.sh`)은 루트 "Apps Script(clasp) 도메인 공통" 섹션으로 옮기고, lead-tracker·leaves 문서는 루트를 가리키게 축약. 새 공통 원칙 **Copy-Paste Lines**(복붙 값은 한 줄에 하나, 이모지·라벨은 같은 줄 허용) 추가.
- **leaves Windows clone**: 이 PC에 `leaves/`가 없어 `mkt-leaves`를 clone, `core.hooksPath` 설정.
- **소크라테스 스킬**: `~/.claude/skills/socratic/` 생성 (ask / close / triple / self). 아포리아 후 자동 close, Claude가 "고칠 곳 없음" 결론을 낼 때 self 모드 자동 실행.
- **온보딩**: 심문 결과 "빈 칸 전부 채우기"가 아니라 실행 레이어(시간·완료 기준 있는 퀘스트)가 빠진 게 핵심으로 정리. `db_onboarding`에 `순서`·`예상 시간(분)` 추가, 완료 기준은 페이지 안 체크리스트로. D1 7개(강의 + 코스별 1:1), D2 6개(강의 + 실습, 1:1 없음), D3 순서 + SSM meet-up 추가. 페이지 틀은 `onboarding/CLAUDE.md`에 기록.
- **writing 도메인 신설**: Copy/Kakao Library 소재 검수·보완, Ideation 작성. Copy는 사용자가 수정 후 원문을 `legacy` 토글에 보관하는 방식. 검수 기준은 Quality Bar(Creative & funnel) 먼저, 애매한 표현은 legacy vs 최종본 차이를 학습 → `writing/copy-review-patterns.md` (4건에서 8개 잠정 패턴). Kakao는 Copy 구축 후.

**미해결 (이월)**
- 온보딩: D2 Growth Meeting 시간(사용자 입력 예정), 채널 강의 자료 미제작, D3·D4 페이지 채우기
- writing: legacy 토글 없이 `isReviewed`된 5건의 의미, Claude 검수 결과를 남기는 방식, Ideation 초안 위치
- Mac: 같은 시각 Mac에서 아이디에이션 미팅 처리 중 → Mac 세션은 push 전에 `git pull` 필요. Mac에는 `~/.claude/skills/socratic/`과 이번 메모리 파일이 없음

## 오전 — Claude Code (Mac): 세션 시작 Git 동기화 실사례, 아이디에이션 미팅 처리

**세션 시작 규칙 누락 확인 및 적용**: "방금 vibe로 녹음했어" 요청에 바로 응답하느라 세션 시작 시 OS·decisions 확인을 건너뜀. 사용자 지적으로 뒤늦게 `git fetch` 실행 → 이 Mac이 origin보다 **8커밋 behind** 상태였음을 발견(오늘 새벽/오전 Windows 세션 분량 전부 미반영, 위 항목들). 로컬 변경 없어 fast-forward pull로 해결. 오늘 오후 새로 생긴 "Session-Start Git Sync Check" 공통 원칙이 정확히 이 상황을 막기 위한 규칙임을 실사례로 확인.

**git identity**: 별도 조치 불필요함을 확인. Mac의 `git config user.email`이 이미 `h.yun@crimsoneducation.org`로 새벽 Windows 세션에서 정한 값과 일치.

**leaves/ 미해결 확인**: 이 Mac에는 `leaves/`(mkt-leaves) 저장소가 아예 클론돼 있지 않음. Windows에는 오늘 오후 clone 완료. 필요해지면 Mac에도 clone 필요 — 아직 실제 작업 요청 없어 보류.

**아이디에이션 미팅 처리**: Vibe 녹음(`Record-2026-09-23 10-15-06`, 15분) 발견 → 전체 한 번에 전사 시 뒷부분에서 hallucination 반복 루프 발생("폴을 잘 만들고 있습니다" 등 수백 회, 9/22와 동일 패턴 — 앱 권장 옵션을 써도 발생 가능함을 재확인). `meetings/CLAUDE.md` 규칙대로 10분 단위 분할 재전사(ffmpeg 없어 Python `wave` 모듈로 직접 분할)로 해결, 반복 없는 깨끗한 텍스트 확보. 문장별 줄바꿈으로 `meetings/ideation/2026-09-23.md` 저장, 커밋+push(`e58df76`) 완료. Notion 페이지는 **미생성** — 사용자가 데스크탑에서 정리 규칙을 직접 정하기로 함.

**다음 액션**: 데스크탑에서 `meetings/ideation/2026-09-23.md` pull 후 정리 규칙 확정 → 확정되면 `meetings/CLAUDE.md`의 Vibe→Notion 매칭 표 갱신 검토.

**`leaves/` Mac 클론 완료**: `https://github.com/Harry-sk-mkt/mkt-leaves.git` → `crimson/leaves/`. `core.hooksPath`를 `.githooks`로 설정(Windows와 동일 pre-commit 훅 적용). clasp는 기존 `~/.clasprc.json` 인증이 이미 있어 별도 로그인 없이 바로 `clasp status` 정상 동작 확인. 이 저장소 자체는 파일 변경 없어(로컬 git config만 설정) 커밋 대상 없음. 이월 항목이던 "Mac leaves 미클론" 해소.

## 오후 (이어서) — Claude Code (Windows): 아이디에이션 미팅 Notion 보강

- Mac 세션 이어받음: 2커밋 behind → fast-forward pull.
- 정리 규칙 확정: 아이디에이션은 새 페이지를 만들지 않고, 발표자가 만들어 둔 `dbTodos` Ideation 페이지(Status=`Brief` 먼저 확인)의 1️⃣ 섹션 맨 아래에 빠진 내용만 `> "인용문"`으로 추가 (그로스미팅과 같은 방식). 본문뿐 아니라 인라인 DB(5W1H/Metrics/ICE)도 대조. `meetings/CLAUDE.md`에 반영.
- 09-23 회의 아이디어 3개 반영: Quality Bar - Activation(5개), 웨비나 사전 녹화(11개), 9.30 WB_CPNP1 9학년 입시 관심도 고려(3개, Brief가 아니라 `On-track_IDEA` 상태였음).
- 제외: 이미 적힌 내용, 인신·건강 관련 잡담.

## 저녁 — Claude Code (NYT 기사 번역 대기, 온보딩 버디 역할)

- **NYT Jamie Beaton 기사 번역**: 한국어 뉴스 스타일 전문 번역 요청 → 저작권 문제로 보류. 사용자가 번역·재게재 허가 컨펌 요청 중. `writing/TODO.md`에 대기 항목으로 기록(착수 전 확인할 용어 목록 포함).
  - 다음 액션: 허가 확인 후 용어 확정 → 번역. 허가 전에는 요약 기사만 가능.
- **온보딩 버디 역할 정의**: Notion 루트 "Reach Person In Charge"의 직무 버디(Professional Buddy, Jennie)·적응 버디(Personal Buddy, Richard) 아래에 역할 토글 추가. 매니저=기대치·평가 / 직무 버디=업무 역량 / 적응 버디=조직·사람 적응으로 구분, 각자 "하지 않는 일" 명시. 사용자가 Notion에서 직접 일부 항목 수정(Tech SETUP 확인, 팀 소개, Slack 초대, 이해관계자 연결 제외, 복지 정책 링크 추가) → `onboarding/buddy-roles.md`에 동기화.
  - 근거: Onboarding Checklist의 Organisational/Technical/Social 항목을 세 역할로 분배.
  - 다음 액션: 매니저 역할 토글은 아직 Notion에 없음(로컬 초안에만 있음). 필요 시 추가.

## 저녁 (이어서) — Claude Code (Mac): 케이스 스터디 미팅 처리, decisions↔CLAUDE.md 동기화 gap 발견

- **케이스 스터디 미팅 신규 처리**: Vibe 녹음(`Record-2026-09-23 14-40-08`, 38분) 발견 → 전사. "케이스 스터디"는 `meetings/CLAUDE.md` 매칭 표의 기존 키워드(1:1/이벤트 리뷰/아이디에이션/그로스/세일즈·마케팅)에 없어 사용자에게 확인: 로컬 저장 위치는 새 폴더 `case-study/`, 일회성/부정기 회의라 `_index.md` 빈도표에는 미반영. 매칭 표에 6번째 행으로 추가, Notion 테이블도 `dbInCorpMeeting`/Tags=Team으로 명시.
- **hallucination 재발 및 처리 지연**: 38분 전체 전사 시 뒷부분(약 4개 구간 중 일부)에서 반복 루프 발생. 사용자에게 어떻게 처리할지 먼저 물어봤으나, 이미 9/22·9/23(아이디에이션)에 10분→2분 단위 분할 재전사로 해결한 전례가 `decisions/2026-09-22.md`, 본 파일 오전 기록에 있었음에도 확인하지 않고 질문한 것을 사용자가 지적. 원인은 9/22 결정 로그에 "`meetings/CLAUDE.md`에 전부 반영"이라 적혀 있었지만 실제로는 분할 재전사 절차 자체가 `meetings/CLAUDE.md`에 빠져 있었던 것 — 이번에 그 gap을 메움(문서에 "hallucination loop 발생 시 분할 재전사로 해결한다" 절 추가).
- **처리 결과**: chunk_00/02는 정상, chunk_01(10~20분)·chunk_03(30~38분)만 2분 단위로 재분할·재전사해 hallucination 제거. 807문장, sentence-per-line 포맷으로 `meetings/case-study/2026-09-23.md` 저장.
- **Notion 페이지 생성**: `dbInCorpMeeting`, Name="케이스 스터디 미팅", Tags=Team, Date=2026-09-23 — https://app.notion.com/p/3e464dfd498f81ac83c7d91d6bd7934d . 템플릿 없어 요약/주요 논의/결정/액션 아이템 구조 + `<details>` 원문 토글로 구성.
- **메모리 갱신**: "결정 로그가 CLAUDE.md에 반영됐다고 적혀 있어도 실제 반영 여부를 확인하지 않고 신뢰하면 안 된다" + "사용자에게 처리 방법을 묻기 전에 `decisions/`에 이미 해결된 전례가 있는지 먼저 검색한다"를 `feedback_check_decisions_before_asking` 메모리로 저장.

**미해결 (이월, 환경 전환 관련)**
- 이 세션에서 만든 `meetings/case-study/2026-09-23.md`, `meetings/CLAUDE.md` 갱신, 본 기록은 아직 push 전 — 다음 세션(다른 컴퓨터)은 pull 전에 반드시 `git fetch` 후 behind 여부 확인.
- 케이스 스터디 미팅이 앞으로도 반복될지 아직 미정 — 반복되면 `_index.md` 빈도표 갱신 필요.

## 밤 — Claude Code (Windows): 케이스 스터디 DB 이전, 슬라이드용 요약 재작성

- **Git**: 맥 커밋 `04222f8`(케이스 스터디 미팅) fast-forward pull.
- **케이스 스터디 저장 위치 변경**: 사용자가 미팅 중 직접 메모를 남기므로 `dbInCorpMeeting`에 신규 페이지를 만들면 안 됨. 앞으로 `db_CaseStudies` 사용. "Brice NYU"(9/22), 맥에서 만든 요약 페이지, "Scarlette 1:1" 메모를 `db_CaseStudies`로 이동(Scarlette 1:1 이동은 권한 차단 → 사용자가 직접 처리, 메모를 맥 요약 페이지에 합쳐 "Scarlette" 페이지로 정리).
- **Tags 변경**: `db_CaseStudies` Tags를 select(회의 유형) → multi-select(US News 2027 National Universities Top 20 동률 포함 22개 + NYU, 랭킹순)로 교체, 기존 값 삭제. 순위는 College Kickstart·검색 결과 두 출처로 교차 확인. Brice NYU = NYU 태그.
- **요약 형식 문제**: 맥 요약이 회의록(요약/논의/결정/액션) 형식이라 세미나 슬라이드에 쓸 수 없다는 피드백 → Scarlette 페이지를 Brice NYU 형식(한 줄 요약/세미나 메시지/학업 지표/합류 전후/활동별/에세이/공개 시 가릴 것/확인 필요)으로 재작성. `meetings/CLAUDE.md` 케이스 스터디 행에 형식·DB·Tags 규칙 반영.

**미해결**
- Scarlette 케이스 학교명: 전사본 "NIU" — NYU 치대로 추정되나 미확인, Tags 비워둠.
- 확인 필요 항목(제주+치대 특정 여부, 연도 공개, 해부학 클럽 현재 규모, 추천 대회 목록 등)은 Scarlette 페이지 하단 참고.
